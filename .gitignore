node_modules/
dist/
release/
mas-build-output/
*.db
*.db-shm
*.db-wal
# Intentional exception: a real committed DB fixture for the upgrade-path
# regression test (tests/upgrade-path.test.ts, specs/DatabaseMigrations.md).
!tests/fixtures/upgrade-snapshots/*.db
backups/
.env
.env.local
.env.*.local
.DS_Store
# WhatsApp linked-device credentials (Baileys auth state) — never commit
whatsapp-auth/

# Apple signing assets — NEVER commit
xcode/
*.cer
*.p12
*.p8
*.key
*.pem
*.certSigningRequest
*.provisionprofile
*.mobileprovision

.next/
brain/

# Private code-review reports and remediation notes — never commit
code-review/

# Private shared docs repo — cloned in by hand, never tracked here (see AGENTS.md)
specs/
test-results/
